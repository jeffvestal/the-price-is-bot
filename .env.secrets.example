# Application Secrets
# Copy this file to .env.secrets and fill in your values
# IMPORTANT: Never commit .env.secrets to git!

# Generate with: openssl rand -base64 32
SECRET_KEY=your_generated_secret_key_for_jwt_signing

# Generate with: openssl rand -base64 32
ADMIN_TOKEN=your_generated_admin_token_for_admin_endpoints
